# Supabase (optional for this MVP, mock fallback when missing)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe (optional for this MVP, mock checkout when missing)
VITE_STRIPE_CHECKOUT_ENDPOINT=

# Optional app config
VITE_PLATFORM_TAKE_RATE=0.2
